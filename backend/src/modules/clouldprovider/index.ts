import * as cloudProviderController from './cloudProvider.controller';
import * as cloudProviderInterfaces from './cloudProvider.interface';
import CloudProvider from './cloudProvider.model';
import * as cloudProviderServices from './cloudProvider.service';

export { cloudProviderController, cloudProviderInterfaces, CloudProvider, cloudProviderServices };
